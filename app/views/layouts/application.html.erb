<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>EventStack</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="EventStack - A social platform for events at Trinity College">
  <meta name="author" content="Vlad Burca">

  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body class='<%= controller.controller_name %>'>

  <div class="navbar navbar-inverse navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to root_path, :class => "brand" do %>
          <span class="trinity">Event</span>Stack
        <% end %>
        <div class="nav-collapse collapse" style="height: 0px">
          <ul class="nav">
            <li class=<%= controller.controller_name.capitalize == "Events" ? "active" : "" %>>
              <%= link_to "Events", events_path %>
            </li>
            <li class=<%= controller.controller_name.capitalize == "Users" ? "active" : "" %>>
              <%= link_to "Users", users_path %>
            </li>
            <% if current_user and current_user.admin? %>
              <li class=<%= controller.controller_name.capitalize == "Majors" ? "active" : "" %>>
                <%= link_to "Majors", majors_path %>
              </li>
            <% end %>
          </ul>

          <ul class="nav pull-right">
            <% if user_signed_in? %>
              <li>
                <p class="navbar-text">
                  <span class="">Hi there,</span> <%= link_to "#{current_user.first_name}", user_path(current_user.id) %>
                </p>
              </li>
              <li>
                <%= link_to "Sign Out", destroy_user_session_path %>
              </li>
            <% else %>
              <li>
                <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => {:class => "navbar-form"}) do |f|%>
                  <%= f.email_field :email, :autofocus => true, :class => "span2", :placeholder => "Email" %>
                  <%= f.password_field :password, :class => "span2", :placeholder => "Password" %>
                  <%= f.submit "Come on in!", :class => "btn" %>
                <% end %>
              </li>
              <li>
                <%= link_to "Want to join?", new_user_registration_path %>
              </li>
            <% end %>
          </ul>

        </div>
      </div>
    </div>
  </div>

  <div class="container">
    <%= yield %>
  </div>

</body>
</html>
